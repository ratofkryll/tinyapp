<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include partials/_header %>
  <h1>Show URLs</h1>
  <ul>
  <% if (longURL[shortURL]) { %>
    <li><%= `${longURL[shortURL]}: ${shortURL}` %></li>
  <% } %>
  </ul>
</body>
</html>
